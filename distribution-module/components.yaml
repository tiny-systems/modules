# Generated by `tools components-info --json` at release. Do not edit by hand.
# Discovery only: name/description/info/tags. Ports come from a running node.
- name: registry_catalog
  description: Registry Catalog
  info: List all repositories and their tags from a container registry. Supports Docker Hub, GHCR, private
    registries, and any OCI-compliant registry. Use to discover images for replication or monitoring.
  tags:
  - OCI
  - Registry
  - Container
  - Catalog
- name: registry_copy
  description: Registry Copy
  info: Copy a container image from one registry to another. Supports auth via dockerConfigJSON (pass
    regcred secret content through edges). Set insecure=true when target has no TLS.
  tags:
  - OCI
  - Registry
  - Container
  - Copy
  - Replicate
