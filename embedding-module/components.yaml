# Generated by `tools components-info --json` at release. Do not edit by hand.
# Discovery only: name/description/info/tags. Ports come from a running node.
- name: embed_text
  description: Embed Text
  info: Calls a HuggingFace text-embeddings-inference (TEI) endpoint and emits the dense embedding vector.
    With the tei bundle enabled on install the TEI service lives in-cluster at http://<release>-tei:80
    and TEI_URL is wired automatically. Override the URL via settings.baseURL for external endpoints.
  tags:
  - Embeddings
  - TEI
  - Vectors
  - RAG
