# Generated by `tools components-info --json` at release. Do not edit by hand.
# Discovery only: name/description/info/tags. Ports come from a running node.
- name: go_template
  description: Go Template Engine
  info: Render templates using text/template standard package. Supports layouts and partials. Output is
    not HTML-escaped, suitable for JSON, plain text, and other formats.
  tags:
  - html
  - template
  - engine
- name: json_decode
  description: JSON Decoder
  info: 'Parses a JSON string into data the rest of the flow can read. SET THE `decoded` SETTING to an
    example of the JSON you expect — a string has no shape, so without one every downstream edge is unverifiable:
    an expression like {{$.decoded.user.id}} is accepted when the flow is built and resolves to null at
    runtime. With an example, the same mistake is caught immediately. Only the shape matters, not the
    values; one representative object is enough, and for a list one representative element. When the payload
    carries a list of items, wire array_split after this so each item arrives as its own message instead
    of every downstream node looping. Some senders report that they truncated a batch, in a field alongside
    it. Include that field in the example and check it, or a batch that silently dropped items reads as
    the complete set.'
  tags:
  - json
- name: json_encode
  description: JSON Encoder
  info: Encodes input document with JSON
  tags:
  - json
- name: jwt_encode
  description: JWT Encoder
  info: Generates JWT token
  tags:
  - jwt
- name: jwt_decode
  description: JWT Decoder
  info: Verifies and decodes JWT token
  tags:
  - jwt
- name: xml_encode
  description: XML Encoder
  info: Encodes input document with XML
  tags:
  - xml
